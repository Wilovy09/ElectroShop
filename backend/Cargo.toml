[package]
name = "backend"
version = "0.1.0"
edition = "2021"

[dependencies]
actix-web = "4.9.0"
dotenvy = "0.15.7"
serde = { version = "1.0.209", features = ["derive"] }
sqlx = { version = "0.8", features = [ "runtime-async-std", "sqlite", "macros", "migrate" ] }

